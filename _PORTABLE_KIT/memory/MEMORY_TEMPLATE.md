# [НАЗВАНИЕ ПРОЕКТА] — Memory

## Workflow (правила сессии)
- Plan mode для задач с 3+ шагами или архитектурными решениями
- Subagents для параллельного анализа — держать main context чистым
- Верификация обязательна перед отметкой задачи выполненной
- После правки → обновить `tasks/lessons.md`
- Планы в `tasks/todo.md` с чекбоксами

## Проект
[Одна строка — суть проекта и что он решает]

## Активный файл
`[путь/к/главному/файлу]` — только его трогаем.
`[другие файлы]` — [статус: активен/заморожен/не трогать]

## Деплой
`[команда деплоя]` — после каждого изменения.

## Архитектура
[Краткое описание как устроен проект: main flow, ключевые компоненты]

## Что можно менять свободно
- [компонент/файл/параметр]
- [компонент/файл/параметр]

## Критические правила
- [что НЕЛЬЗЯ трогать и почему]
- [пороги/значения которые нельзя изменять]

## Предпочтения пользователя
- Насыщенные, живые [результаты] — склоняться к большей выразительности
- Стиль общения: неформальный, мат уместен

## Структура папок
- `src/` — исходники
- `docs/` — документация, бэкапы
- `tasks/` — todo.md, lessons.md
- корень: install/build скрипты, README.md, CLAUDE.md

## Правило патчнота
После видимых изменений → добавить блок в начало `docs/PATCHNOTES.md`
Формат: `## Патч vX.X — YYYY-MM-DD`. Тон: неформальный, только пользовательский эффект.

## Известные проблемы / history
[Заполнять по мере работы — критические поломки и их причины]

## Откат при поломке
```
[команда отката к prev]
[команда отката к backup]
```

# currentDate
Today's date is [YYYY-MM-DD].
